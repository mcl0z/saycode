# saycode
SayCoding is a terminal-first AI coding assistant built on multi-agent collaboration. Its main agent breaks down tasks and dispatches sub-agents to work in parallel. Users observe real-time collaboration, exchange messages, grant permissions on demand, and resume sessions—all within the terminal.
